services:
  bot:
    build: .
    container_name: conflict-resolution-assistant
    restart: unless-stopped
    env_file:
      - .env
      - /home/openclaw/.config/openclaw/openclaw.env
    volumes:
      - ./data:/app/data
